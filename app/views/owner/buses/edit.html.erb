<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h1 class="text-center">Editing Bus</h1>

      <%= render "form", bus: @bus %>

      <div class="text-center mt-3">
        <%= link_to "Show this bus", owner_bus_path(@bus), class: 'btn btn-info mx-2' %>
        <%= link_to "Back to Buses", owner_buses_path, class: 'btn btn-secondary' %>
      </div>
    </div>
  </div>
</div>

