<nav class="navbar navbar-expand-lg navbar-light bg-primary">
   <%= link_to buses_path do %>
    <%= image_tag("bus_image.png", alt: "Logo", class: "logo-img", size: "100x70")%>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
      <% if current_user && current_user.bus_owner? %>
       <!--  <li class="nav-item active">
          <%= link_to "Add Bus", new_owner_bus_path,data: { turbo_frame: "second_frame" }, class: "nav-link text-white" %>
        </li> -->
        <li class="nav-item">
          <%= link_to "My Buses", owner_buses_path, class: "nav-link text-white" %>
        </li>
      <%end%>
      <% if current_user %>
        <li class="nav-item">
          <%= link_to "My Reservations", reservations_path, class: "nav-link text-white" %>
        </li>
      <%end%> 
      
    </ul>
    <span class="navbar-text">
      <% if user_signed_in? %>
        <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "btn btn-light" %>
        <%= link_to "Edit Profile", edit_user_registration_path, class: "btn btn-light" %>
      <% else %>
        <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-light" %>
        <%= link_to "Sign In", new_user_session_path, class: "btn btn-light" %>
      <% end %>
    </span>
  </div>
</nav>

<style>
.logo-img{
  filter: invert(1);
}
</style>